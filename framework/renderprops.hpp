#ifndef RENDERPROPS_HPP
#define RENDERPROPS_HPP

#include <string>

struct RenderProps {
    std::string cam_name;
    std::string filename;
    int x_res;
    int y_res;
};

#endif // RENDERPROPS_HPP